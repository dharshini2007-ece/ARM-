#include <lpc214x.h>
void delay(unsigned int);
int main()
{
	PINSEL0 =0X00000000;
	IODIR0 =IODIR0|(1<<4);
	IODIR0 =IODIR0|(1<<5);
	IODIR0 =IODIR0|(1<<6);
	IODIR0 =IODIR0|(1<<7);
	while(1)
	{
		IOCLR0 =IOCLR0|(1<<4)|(1<<5)|(1<<6)|(1<<7);
		IOSET0 =IOSET0|(1<<4);
		delay(50000);
		IOCLR0 =IOCLR0|(1<<4)|(1<<6)|(1<<7);
		IOSET0 =IOSET0|(1<<5);
		delay(50000);
		IOCLR0 =IOCLR0|(1<<4)|(1<<5)|(1<<7);
		IOSET0 =IOSET0|(1<<6);
		delay(50000);
		IOCLR0 =IOCLR0|(1<<4)|(1<<5)|(1<<6);
		IOSET0 =IOSET0|(1<<7);
		delay(50000);
	}
}

void delay(unsigned int dly)
{
	int i,j;
	for(i=0;i<dly;i++)
	{
		for(j=0;j<100;j++);
		{
		}
	}
}
		
		
		
