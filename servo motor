#include <lpc214x.h>
void delay(unsigned int dl)
{
	int i;
	while(dl--)
	{
		for(i=0;i<1275;i++)
		{
		}
	}
}
int main()
{
	PINSEL0=0X00000000;
	IODIR0 =IODIR0|(1<<3);
	while(1)
	{
		IOCLR0 = IOCLR0|(1<<3);
		delay(100);
		IOSET0 =IOSET0|(1<<3);
		delay(100);
	}
}
	
